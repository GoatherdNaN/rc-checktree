// 传递方向
export const DIRECTION = {
  UPWARD_MODE : 'upward',
  DOWNWARD_MODE : 'downward'
};

// 状态, 半选state也为true
export const STATE = {
  unchecked: 0, // 取消选中
  checked: 1, // 选中
  halfChecked: 2, // 半选
};

// 初始值parentKey
export const INIT_PARENT_KEY = 0;